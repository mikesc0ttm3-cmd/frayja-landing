# images

This folder contains image assets used by the Frayja landing site. Add optimized images here (prefer WebP for photos and SVG for icons/logos). Use lowercase, hyphen-separated filenames (e.g. hero-banner.webp). Do not commit large binaries; use Git LFS or external hosting for files over ~50 MB.

If you want me to add specific image files, provide the files or URLs and I will add them to this folder.
